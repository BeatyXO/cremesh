import ProtocolPage from '@/components/ProtocolPage';
export default function Targets(){return <ProtocolPage eyebrow="TARGETS & POLICIES / LIVE" title="Scoped policy records"><div className="empty"><h3>Targets and policy history</h3><p>Each target owns versioned policies with SHA-256 hashes. Enroll and publish through the deployed contract.</p></div></ProtocolPage>}
